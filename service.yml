kind: Service
apiVersion: v1
metadata: 
  name: nginx-service
  namespace: nginx
spec:
  clusterIP: None
  selector:
    app: nginx
  ports: 
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
